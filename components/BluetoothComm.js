import { StyleSheet, Text, View } from "react-native";

const BluetoothComm = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.h1}>Method 2: Bluetooth Communication</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
  h1: {
    fontSize: 18,
    fontWeight: "bold",
  },
});

export default BluetoothComm;
